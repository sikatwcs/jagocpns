{
  "name": "jagocpns",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm --prefix client run dev",
    "build": "npm --prefix client run build",
    "build:vercel": "npm --prefix client install && npm --prefix client run build",
    "preview": "npm --prefix client run preview",
    "lint": "npm --prefix client run lint"
  },
  "engines": {
    "node": ">=20"
  }
}
